local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Select Scripts",
    SubTitle = "pastebin.com/u/ty4rds",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true, -- The blur may be detectable, setting this to false disables blur entirely
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when theres no MinimizeKeybind
})

--Fluent provides Lucide Icons https://lucide.dev/icons/ for the tabs, icons are optional
local Tabs = {
    Main = Window:AddTab({ Title = "Mobile", Icon = "airplay" }),
    Vip = Window:AddTab({ Title = "PC", Icon = "box" }),
	Upd = Window:AddTab({ Title = "NEW UPDATE", Icon = "check" }),
    New = Window:AddTab({ Title = "Credit", Icon = "hammer" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local Options = Fluent.Options

do end
    Fluent:Notify({
        Title = "Notification Alert",
        Content = "Message From IOER HUB",
        SubContent = "Thank You For Use This Script", -- Optional
        Duration = 5 -- Set to nil to make the notification not disappear
    })



    Tabs.Main:AddParagraph({
        Title = "Intruction",
        Content = "This is For Mobile User"
    })

    Tabs.Vip:AddParagraph({
        Title = "Intruction",
        Content = "This Is PREMIUM Version"
    })

    Tabs.Upd:AddParagraph({
        Title = "Intruction",
        Content = "Latest Updates Are Here!"
    })

    Tabs.New:AddParagraph({
        Title = "[Builder]",
        Content = "Owner IOER HUB"
    })

    Tabs.New:AddParagraph({
        Title = "[Programmer]",
        Content = "Owner IOER HUB"
    })

    Tabs.New:AddParagraph({
        Title = "[Idea]",
        Content = "Faren/rex_del2"
    })

    Tabs.New:AddParagraph({
        Title = "[Link Script]",
        Content = "pastebin.com/u/ty4rds"
    })

    Tabs.New:AddParagraph({
        Title = "[Script Search]",
        Content = "Youtube | Pastebin.com | Scriptblox | Rscripts | Google"
    })

    Tabs.New:AddParagraph({
        Title = "[Member Premium IOER HUB]",
        Content = "Faren | Ilyas | Zafran | Dicky"
    })

    Tabs.New:AddParagraph({
        Title = "[important information]",
        Content = "This script is still in testing!, if there are any bugs or anything like that, sorry."
    })


    Tabs.Main:AddButton({
        Title = "PREMIUM",
        Description = "100% Script And Keyless",
        Callback = function()
            Window:Dialog({
                Title = "Infinite yield",
                Content = "PowerFull Script",
                Buttons = {loadstring(game:HttpGet("https://raw.githubusercontent.com/IOERHUB/Rayfield/refs/heads/main/Mobile"))()
                    {
                        Title = "Confirm",
                        Callback = function(v)
                            print("Next Update,Plase Wait")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function(v)
                            print("Cancelled,Are You Sure?")
                        end
                    }
                }
            })
        end
    })

    Tabs.Main:AddButton({
        Title = "FREE",
        Description = "50% Script, Coming Soon!",
        Callback = function()
            Window:Dialog({
                Title = "Coming Soon!",
                Content = "Please wait, Until updated!",
                Buttons = {
                    {
                        Title = "Confirm",
                        Callback = function(v)
                            print("Next Update,Plase Wait")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function(v)
                            print("Cancelled,Are You Sure?")
                        end
                    }
                }
            })
        end
    })


    Tabs.Vip:AddButton({
        Title = "PREMIUM",
        Description = "100% IOER HUB Script! And keyless",
        Callback = function()
            Window:Dialog({
                Title = "Infinite yield",
                Content = "PowerFull Script",
                Buttons = {loadstring(game:HttpGet("https://raw.githubusercontent.com/IOERHUB/PREMIUM/refs/heads/main/KEYS%20SYSTEM"))()
                    {
                        Title = "Confirm",
                        Callback = function(v)
                            print("Next Update,Plase Wait")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function(v)
                            print("Cancelled,Are You Sure?")
                        end
                    }
                }
            })
        end
    })

    Tabs.Vip:AddButton({
        Title = "FREE-Release",
        Description = "Beta Tester Script!",
        Callback = function()
            Window:Dialog({
                Title = "Infinite yield",
                Content = "PowerFull Script",
                Buttons = {loadstring(game:HttpGet("https://raw.githubusercontent.com/IOERHUB/Main/refs/heads/main/Windows"))()
                    {
                        Title = "Confirm",
                        Callback = function(v)
                            print("Next Update,Plase Wait")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function(v)
                            print("Cancelled,Are You Sure?")
                        end
                    }
                }
            })
        end
    })

    Tabs.Upd:AddButton({
        Title = "UPDATE 1",
        Description = "100% FREE And No Key",
        Callback = function()
            Window:Dialog({
                Title = "Infinite yield",
                Content = "PowerFull Script",
                Buttons = {loadstring(game:HttpGet("https://raw.githubusercontent.com/IOERHUB/Update/refs/heads/main/IOERHUB"))()
                    {
                        Title = "Confirm",
                        Callback = function(v)
                            print("Next Update,Plase Wait")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function(v)
                            print("Cancelled,Are You Sure?")
                        end
                    }
                }
            })
        end
    })

-- SaveManager (Allows you to have a configuration system)end
-- InterfaceManager (Allows you to have a interface managment system)

-- Hand the library over to our managers
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

-- Ignore keys that are used by ThemeManager.
-- (we dont want configs to save themes, do we?)
SaveManager:IgnoreThemeSettings()

-- You can add indexes of elements the save manager should ignore
SaveManager:SetIgnoreIndexes({})

-- use case for doing it this way:
-- a script hub could have themes in a global folder
-- and game configs in a separate folder per game
InterfaceManager:SetFolder("FluentScriptHub")
SaveManager:SetFolder("FluentScriptHub/specific-game")

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)


Window:SelectTab(1)

Fluent:Notify({
    Title = "Information",
    Content = "The script has been loaded.",
    Duration = 8
})

-- You can use the SaveManager:LoadAutoloadConfig() to load a config
-- which has been marked to be one that auto loads!
SaveManager:LoadAutoloadConfig()

print("Upgrade Your Script To Be Able To Use 100% Of The Script")
print("Created By IOER Hub, pastebin.com/u/ty4rds")
